<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Configuración - VentasAlex 🛒</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f1f3f5;
      }
      .container {
        max-width: 600px;
        margin-top: 40px;
      }
      .form-label {
        font-weight: 600;
      }
      .form-control,
      .form-select {
        border-radius: 10px;
      }
      .card {
        border-radius: 15px;
        box-shadow: 0 0 15px rgba(0, 0, 0, 0.05);
      }
      @media (max-width: 576px) {
        h2 {
          font-size: 1.5rem;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="card p-4">
        <h2 class="text-center text-primary mb-4">
          <i class="bi bi-gear-fill"></i> Configuración General
        </h2>

        <% if (mensajeExito) { %>
        <div class="alert alert-success"><%= mensajeExito %></div>
        <% } %> <% if (mensajeError) { %>
        <div class="alert alert-danger"><%= mensajeError %></div>
        <% } %>

        <form action="/configuracion/guardar" method="POST">
          <div class="mb-3">
            <label for="nombre_empresa" class="form-label"
              >Nombre de la Empresa</label
            >
            <input
              type="text"
              id="nombre_empresa"
              name="nombre_empresa"
              class="form-control"
              required
              value="<%= configuracion.nombre_empresa %>"
            />
          </div>

          <div class="mb-3">
            <label for="ruc" class="form-label">RUC</label>
            <input
              type="text"
              id="ruc"
              name="ruc"
              class="form-control"
              required
              value="<%= configuracion.ruc %>"
            />
          </div>

          <div class="mb-3">
            <label for="direccion" class="form-label">Dirección</label>
            <input
              type="text"
              id="direccion"
              name="direccion"
              class="form-control"
              value="<%= configuracion.direccion %>"
            />
          </div>

          <div class="mb-3">
            <label for="telefono" class="form-label">Teléfono</label>
            <input
              type="text"
              id="telefono"
              name="telefono"
              class="form-control"
              value="<%= configuracion.telefono %>"
            />
          </div>

          <div class="mb-3">
            <label for="email" class="form-label">Correo Electrónico</label>
            <input
              type="email"
              id="email"
              name="email"
              class="form-control"
              value="<%= configuracion.email %>"
            />
          </div>

          <div class="mb-3">
            <label for="porcentaje_igv" class="form-label">IGV (%)</label>
            <input
              type="number"
              step="0.01"
              id="porcentaje_igv"
              name="porcentaje_igv"
              class="form-control"
              required
              value="<%= configuracion.porcentaje_igv %>"
            />
          </div>

          <div class="mb-3">
            <label for="mensaje_pie" class="form-label"
              >Mensaje al pie de comprobante</label
            >
            <textarea
              id="mensaje_pie"
              name="mensaje_pie"
              class="form-control"
              rows="3"
            >
<%= configuracion.mensaje_pie %></textarea
            >
          </div>

          <div class="mb-3">
            <label for="logo_url" class="form-label">URL del Logo</label>
            <input
              type="text"
              id="logo_url"
              name="logo_url"
              class="form-control"
              value="<%= configuracion.logo_url %>"
            />
          </div>

          <div class="text-center">
            <button type="submit" class="btn btn-primary">
              <i class="bi bi-save-fill"></i> Guardar Cambios
            </button>
            <a href="/" class="btn btn-secondary ms-2">Cancelar</a>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
