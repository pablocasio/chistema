<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Perfil del Usuario</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      .nombre-usuario {
        font-size: 2.5rem;
        font-weight: 700;
      }
      .img-perfil {
        max-width: 150px;
        max-height: 150px;
        object-fit: cover;
        border-radius: 50%;
        border: 3px solid #198754; /* verde bootstrap */
      }
    </style>
  </head>
  <body class="bg-light">
    <div class="container mt-5">
      <div class="card shadow-sm p-4">
        <div class="text-center mb-4">
          <div class="nombre-usuario mb-2"><%= usuario.nombre_usuario %></div>
          <!-- Imagen: si tienes foto en perfil.foto, la muestras, sino placeholder -->
          <% if (perfil && perfil.foto) { %>
          <img
            src="<%= perfil.foto %>"
            alt="Foto de perfil"
            class="img-perfil"
          />
          <% } else { %>
          <img
            src="https://via.placeholder.com/150?text=Sin+Foto"
            alt="Sin foto"
            class="img-perfil"
          />
          <% } %>
        </div>

        <div class="row">
          <!-- Datos importantes -->
          <div class="col-md-6 mb-3">
            <p><strong>DNI:</strong> <%= usuario.dni %></p>
            <p><strong>Email:</strong> <%= usuario.email %></p>
            <p>
              <strong>Rol:</strong> <%= usuario.rol === 'admin' ?
              'Administrador' : 'Cajero' %>
            </p>
            <p>
              <strong>Fecha de Registro:</strong> <%= new
              Date(usuario.fecha_creacion).toLocaleDateString() %>
            </p>
            <p><strong>Estado:</strong> <%= usuario.estado %></p>
          </div>

          <!-- Datos adicionales -->
          <div class="col-md-6 mb-3">
            <p>
              <strong>Teléfono:</strong>
              <% if (perfil && perfil.telefono) { %> <%= perfil.telefono %> <% }
              else { %>
              <span class="text-muted">No proporcionado</span>
              <% } %>
            </p>

            <p>
              <strong>Dirección:</strong>
              <% if (perfil && perfil.direccion) { %> <%= perfil.direccion %> <%
              } else { %>
              <span class="text-muted">No proporcionada</span>
              <% } %>
            </p>

            <p>
              <strong>Fecha de nacimiento:</strong>
              <% if (perfil && perfil.fecha_nacimiento) { %> <%= new
              Date(perfil.fecha_nacimiento).toLocaleDateString() %> <% } else {
              %>
              <span class="text-muted">No proporcionada</span>
              <% } %>
            </p>

            <p>
              <strong>Último inicio de sesión:</strong>
              <% if (perfil && perfil.ultimo_login) { %> <%= new
              Date(perfil.ultimo_login).toLocaleString() %> <% } else { %>
              <span class="text-muted">No registrado</span>
              <% } %>
            </p>
          </div>
        </div>

        <div class="text-center mt-4">
          <a href="/perfil/editar" class="btn btn-warning btn-lg px-4">
            ✏️ Editar perfil
          </a>
        </div>

        <div class="text-center mt-3">
          <a href="/deshboard" class="btn btn-outline-primary">
            Volver al inicio
          </a>
        </div>
      </div>
    </div>
  </body>
</html>
