<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>Listado de Clientes</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f8f9fa;
      }

      h1 {
        font-weight: 700;
      }

      .badge {
        font-weight: 600;
        font-size: 0.9rem;
        padding: 0.4em 0.7em;
        border-radius: 0.375rem;
      }

      .form-control {
        height: 38px;
        font-size: 0.95rem;
        border-radius: 0.375rem;
      }

      .form-control:focus {
        border-color: #0d6efd;
        box-shadow: 0 0 0 0.2rem rgba(13, 110, 253, 0.25);
      }

      .btn {
        min-width: 130px;
        font-weight: 600;
      }

      .table-responsive {
        overflow-x: auto;
      }

      table.table {
        border-radius: 0.5rem;
        overflow: hidden;
        box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
      }

      @media (max-width: 767.98px) {
        h1 {
          font-size: 1.5rem;
        }

        .btn,
        .form-control {
          font-size: 0.9rem;
        }

        .top-actions {
          flex-direction: column !important;
          align-items: stretch !important;
        }

        .top-actions .btn {
          width: 100%;
        }

        .search-actions {
          flex-direction: column !important;
          align-items: stretch !important;
        }

        .search-actions button,
        .search-actions a {
          width: 100%;
        }
      }
    </style>
  </head>
  <body>
    <div class="container mt-5">
      <!-- Título y botones superiores -->
      <div
        class="d-flex justify-content-between align-items-center mb-4 flex-wrap gap-3 top-actions"
      >
        <h1 class="text-primary mb-0">👥 Listado de Clientes</h1>
        <div class="d-flex flex-wrap gap-2">
          <a href="/clientes/registrar" class="btn btn-success"
            >➕ Registrar Cliente</a
          >
          <a href="/deshboard" class="btn btn-outline-secondary"
            >🔙 Volver al Dashboard</a
          >
        </div>
      </div>

      <!-- Filtro de búsqueda -->
      <form method="get" action="/clientes/buscar" class="row g-3 mb-3">
        <div class="col-12 col-md-6">
          <input
            type="text"
            name="q"
            class="form-control"
            placeholder="Nombre, apellido o documento"
          />
        </div>
        <div
          class="col-12 d-flex justify-content-between flex-wrap gap-2 search-actions"
        >
          <button type="submit" class="btn btn-primary">🔍 Buscar</button>
          <a href="/clientes/exportar/excel" class="btn btn-outline-success"
            >📥 Exportar Excel</a
          >
          <a href="/clientes/exportar/pdf" class="btn btn-outline-danger"
            >📄 Exportar PDF</a
          >
        </div>
      </form>

      <!-- Tabla de clientes -->
      <% if (clientes && clientes.length > 0) { %>
      <div class="table-responsive">
        <table
          class="table table-bordered table-hover align-middle bg-white text-nowrap"
        >
          <thead class="table-dark text-center">
            <tr>
              <th>ID</th>
              <th>Tipo Doc</th>
              <th>N° Documento</th>
              <th>Nombre</th>
              <th>Apellido Paterno</th>
              <th>Apellido Materno</th>
              <th>Cod. Verif.</th>
              <th>Dirección</th>
              <th>Teléfono</th>
              <th>Correo</th>
              <th>Categoría</th>
              <th>Acciones</th>
            </tr>
          </thead>
          <tbody>
            <% clientes.forEach(cliente => { %>
            <tr>
              <td class="text-center"><%= cliente.id %></td>
              <td class="text-center">
                <span class="badge bg-info"><%= cliente.tipo_documento %></span>
              </td>
              <td class="text-center fw-semibold"><%= cliente.documento %></td>
              <td><%= cliente.nombres %></td>
              <td><%= cliente.apellido_paterno %></td>
              <td><%= cliente.apellido_materno %></td>
              <td class="text-center">
                <%= cliente.codigo_verificacion || "-" %>
              </td>
              <td><%= cliente.direccion %></td>
              <td><%= cliente.telefono %></td>
              <td><%= cliente.email %></td>
              <td class="text-center">
                <% if (cliente.categoria === "Nuevo") { %>
                <span class="badge bg-secondary">Nuevo</span>
                <% } else if (cliente.categoria === "Frecuente") { %>
                <span class="badge bg-primary">Frecuente</span>
                <% } else if (cliente.categoria === "VIP") { %>
                <span class="badge bg-warning text-dark">VIP</span>
                <% } else { %>
                <span class="badge bg-light text-dark">Sin categoría</span>
                <% } %>
              </td>
              <td class="text-center">
                <a
                  href="/clientes/editar/<%= cliente.id %>"
                  class="btn btn-sm btn-warning"
                  >✏️ Editar</a
                >
                <a
                  href="/clientes/eliminar/<%= cliente.id %>"
                  class="btn btn-sm btn-danger"
                  onclick="return confirm('¿Seguro de eliminar este cliente?')"
                  >🗑️ Eliminar</a
                >
              </td>
            </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
      <% } else { %>
      <div class="alert alert-info text-center">
        No hay clientes registrados.
      </div>
      <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
